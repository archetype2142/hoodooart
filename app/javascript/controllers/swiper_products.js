import { Controller } from "@hotwired/stimulus"

// Connects to data-controller="controller-name"
export default class extends Controller {
  connect() {
    // var main_image = document.querySelector('#main_image');

    // var thumbnails = document.querySelectorAll('.image-thumbnail');

    // thumbnails.forEach(thumbnail => {
    //   thumbnail.addEventListener('click', function(elem) {
    //     main_image.src = elem.currentTarget.src
    //   })
    // })
  }
}
